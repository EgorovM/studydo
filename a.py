a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))
a, b, c = Bl / Nl, 0, l[0][0]
min_mse = left_mse()


for i, (x, y) in enumerate(l):
    #left
    Nl -= 1
    Al -= y**2
    Bl -= y

    #right
    Nr += 1
    Ar += y**2
    Br += y
    #print('x = ', x, min_mse)

    if i != len(l) - 1 and l[i+1][0] == x:
        continue

    #print('mse:', min_mse, a, b, c, (x, y))

    if left_mse() + right_mse() < min_mse:
        c = x
        a = Bl/Nl
        b = Br/Nr

        min_mse = left_mse() + right_mse()

print("%.6f %.6f %.6f" % (a, b, c))

